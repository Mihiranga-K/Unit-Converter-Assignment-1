version: '3.8'
services:
  unit-converter:
    build: .
    container_name: unit_converter_app
    ports:
      - "8080:80"
    networks:
      - app-network
    deploy:
      resources:
        limits:
          memory: 512M

networks:
  app-network:
    driver: bridge